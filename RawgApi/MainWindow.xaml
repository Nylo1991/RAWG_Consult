<Window x:Class="RawgApi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Painel do Aluno 1 - Captura RAWG" Height="450" Width="800">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="40"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Buscar Jogo:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Width="250" Height="25" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
            <Button Content="Buscar na RAWG" Width="120" Height="25" Command="{Binding FetchFromRawgCommand}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding GamesList}" AutoGenerateColumns="True" IsReadOnly="True" RenderTransformOrigin="0.533,0.45" Margin="-5547,-3413,-10412,-3433" Grid.RowSpan="3" >
            <DataGrid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="1.573"/>
                    <TranslateTransform X="9.743" Y="15.301"/>
                </TransformGroup>
            </DataGrid.RenderTransform>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center" FontWeight="Bold" Foreground="DarkBlue"/>

            <Button Grid.Column="1" Content="Salvar no SQLite" Width="120" Height="30" Margin="0,0,10,0" Command="{Binding SaveLocalCommand}"/>
            <Button Grid.Column="2" Content="Enviar para Nuvem (API)" Width="150" Height="30" Background="#005A9E" Foreground="White" Command="{Binding SendToBackendCommand}"/>
        </Grid>
    </Grid>
</Window>