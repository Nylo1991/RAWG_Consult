<Window x:Class="RawgApi.Views.BancoLocalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Meu Banco Local"
        Height="700"
        Width="1250"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TOPO -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="📂 Banco de Jogos Local"
                       FontSize="30"
                       FontWeight="Bold"
                       Foreground="White"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Content="✕"
                    Width="45"
                    Height="45"
                    FontSize="22"
                    FontWeight="Bold"
                    Background="#C0392B"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="Fechar_Click"/>
        </Grid>

        <!-- BOTÕES -->
        <Border Grid.Row="1"
                Background="#2D2D30"
                CornerRadius="12"
                Padding="15"
                Margin="0,0,0,20">

            <WrapPanel>

                <Button Content="🔄 Carregar Banco"
                        Command="{Binding CarregarBancoCommand}"
                        FontSize="15"
                        FontWeight="Bold"
                        Background="#34495E"
                        Foreground="White"
                        BorderThickness="0"
                        Height="45"
                        Width="170"
                        Margin="5"/>

                <Button Content="✏ Atualizar"
                        Command="{Binding AtualizarCommand}"
                        FontSize="15"
                        FontWeight="Bold"
                        Background="#F39C12"
                        Foreground="White"
                        BorderThickness="0"
                        Height="45"
                        Width="130"
                        Margin="5"/>

                <Button Content="🗑 Excluir"
                        Command="{Binding ExcluirCommand}"
                        FontSize="15"
                        FontWeight="Bold"
                        Background="#C0392B"
                        Foreground="White"
                        BorderThickness="0"
                        Height="45"
                        Width="120"
                        Margin="5"/>

                <Button Content="☑ Selecionar Todos"
                        Command="{Binding SelecionarTodosCommand}"
                        FontSize="15"
                        FontWeight="Bold"
                        Background="#7F8C8D"
                        Foreground="White"
                        BorderThickness="0"
                        Height="45"
                        Width="170"
                        Margin="5"/>

                <Button Content="🗑 Excluir Marcados"
                        Command="{Binding ExcluirMarcadosCommand}"
                        FontSize="15"
                        FontWeight="Bold"
                        Background="#922B21"
                        Foreground="White"
                        BorderThickness="0"
                        Height="45"
                        Width="170"
                        Margin="5"/>

                <Button Content="☁ Enviar Nikolas"
                        Command="{Binding EnviarApiCommand}"
                        FontSize="15"
                        FontWeight="Bold"
                        Background="#8E44AD"
                        Foreground="White"
                        BorderThickness="0"
                        Height="45"
                        Width="140"
                        Margin="5"/>

            </WrapPanel>
        </Border>

        <!-- TABELA -->
        <Border Grid.Row="2"
                Background="#2D2D30"
                CornerRadius="12"
                Padding="10">

            <DataGrid ItemsSource="{Binding JogosSalvos}"
                      SelectedItem="{Binding JogoSelecionado, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      Background="#2D2D30"
                      Foreground="White"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      RowHeight="70"
                      FontSize="14"
                      IsReadOnly="False"
                      CanUserAddRows="False"
                      RowBackground="#33373E"
                      AlternatingRowBackground="#252526"
                      AlternationCount="2"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">

                <DataGrid.Resources>

                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#007ACC"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="FontSize" Value="15"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>

                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="6"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="White"/>
                    </Style>

                </DataGrid.Resources>

                <DataGrid.Columns>

                    <DataGridTemplateColumn Header="✓" Width="45">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Capa" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="6"
                                        ClipToBounds="True"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                    <Image Source="{Binding ImagemUrl}"
                                           Width="95"
                                           Height="55"
                                           Stretch="UniformToFill"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="ID Local"
                                        Binding="{Binding DisplayId}"
                                        Width="90"
                                        IsReadOnly="True"/>

                    <DataGridTemplateColumn Header="Nome" Width="280">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Nome}"
                                           VerticalAlignment="Center"
                                           Foreground="White"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding Nome}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Descrição" Width="260">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Descricao}"
                                           VerticalAlignment="Center"
                                           Foreground="White"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding Descricao}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Avaliação"
                                        Binding="{Binding Avaliacao}"
                                        Width="100"/>

                    <DataGridTextColumn Header="Metacritic"
                                        Binding="{Binding Classificacao}"
                                        Width="110"/>

                    <DataGridTextColumn Header="Upload"
                                        Binding="{Binding Upload, StringFormat=dd/MM/yyyy HH:mm}"
                                        Width="160"
                                        IsReadOnly="True"/>

                </DataGrid.Columns>

            </DataGrid>
        </Border>

        <!-- STATUS -->
        <Border Grid.Row="3"
                Margin="0,15,0,0"
                Background="#007ACC"
                CornerRadius="10"
                Padding="12">

            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="16"
                       Foreground="White"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"/>
        </Border>

    </Grid>
</Window>