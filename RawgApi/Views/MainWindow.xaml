<Window x:Class="RawgApi.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="RAWG Explorer"
        Height="700"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TÍTULO -->
        <TextBlock Text="🎮 RAWG GAME EXPLORER"
                   FontSize="32"
                   FontWeight="Bold"
                   Foreground="White"
                   Margin="0,0,0,20"/>

        <!-- ÁREA DE PESQUISA -->
        <Border Grid.Row="1"
                Background="#2D2D30"
                CornerRadius="12"
                Padding="15"
                Margin="0,0,0,20">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>

                <!-- TEXTBOX -->
                <TextBox Grid.Column="0"
                         Height="45"
                         FontSize="18"
                         VerticalContentAlignment="Center"
                         Padding="10"
                         Text="{Binding TermPesquisa, UpdateSourceTrigger=PropertyChanged}"
                         Background="#3C3C3C"
                         Foreground="White"
                         BorderThickness="0"/>

                <!-- BOTÃO BUSCAR -->
                <Button Grid.Column="1"
                        Margin="10,0"
                        Content="🔍 Buscar RAWG"
                        FontSize="16"
                        Command="{Binding FetchFromRawgCommand}"
                        Background="#007ACC"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>

                <!-- BOTÃO SQLITE -->
                <Button Grid.Column="2"
                        Margin="10,0"
                        Content="💾 Salvar SQLite"
                        FontSize="16"
                        Command="{Binding SaveToLocalDbCommand}"
                        Background="#16A085"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>

                <!-- BOTÃO ENVIO -->
                <Button Grid.Column="3"
                        Margin="10,0"
                        Content="☁ Enviar API"
                        FontSize="16"
                        Command="{Binding SendToAluno2Command}"
                        Background="#8E44AD"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- TABELA -->
        <Border Grid.Row="2"
                Background="#2D2D30"
                CornerRadius="12"
                Padding="10">

            <DataGrid ItemsSource="{Binding ListaGames}"
                      AutoGenerateColumns="False"
                      Background="#2D2D30"
                      Foreground="White"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      BorderThickness="0"
                      RowHeight="40"
                      FontSize="15"
                      IsReadOnly="True">

                <DataGrid.Resources>

                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#007ACC"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Height" Value="40"/>
                    </Style>

                </DataGrid.Resources>

                <DataGrid.Columns>

                    <DataGridTextColumn Header="ID"
                                        Binding="{Binding Id}"
                                        Width="80"/>

                    <DataGridTextColumn Header="Nome"
                                        Binding="{Binding Name}"
                                        Width="300"/>

                    <DataGridTextColumn Header="Lançamento"
                                        Binding="{Binding Released}"
                                        Width="150"/>

                    <DataGridTextColumn Header="Nota"
                                        Binding="{Binding Rating}"
                                        Width="100"/>

                    <DataGridTextColumn Header="Metacritic"
                                        Binding="{Binding Metacritic}"
                                        Width="120"/>

                    <DataGridTextColumn Header="Reviews"
                                        Binding="{Binding ReviewsCount}"
                                        Width="120"/>

                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- STATUS -->
        <Border Grid.Row="3"
                Margin="0,15,0,0"
                Background="#007ACC"
                CornerRadius="10"
                Padding="10">

            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="16"
                       Foreground="White"
                       FontWeight="SemiBold"/>
        </Border>

    </Grid>
</Window>